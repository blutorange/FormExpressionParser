function arrayMax(array) {
  len = array.length;
  if (len == 0) throw exception("array is empty");
  max = array[0];
  for (i = 1; i < len; i+=1) {
    if (array[i] > max) max = array[i];
  }
  max;
}

arrayMax([4,3,1,99,7]);