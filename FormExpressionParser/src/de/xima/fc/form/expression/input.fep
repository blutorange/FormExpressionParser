global scope {
  method<void, string> bar;
} 
function void foo(string x) {
  return x;
} 
bar=foo;
  
  
function boolean bar(number x) {

  if (x==0)
    return false;
  else
    x *= 2;
    
  number y = 1/x;

  if (y < 2)
    y + 5;
  else
    y + 2;

  while (true) {
    if (whatever) break;
    6;
  }
    
} 