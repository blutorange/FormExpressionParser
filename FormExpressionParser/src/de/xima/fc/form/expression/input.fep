global scope {
  array<number> arr = [0,1,2,3];
  method<number, number, number> comparator = ->number(number x, number y){y - x;};
  method<array<number>, number, number...> push;
}
//arr.get(false);
push = arr.push;
//arr.sortBy(comparator);